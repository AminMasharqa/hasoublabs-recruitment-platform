"""Profiles domain module (R4, R4A).

Public surface area for other modules:
- ``app.modules.profiles.api``: :class:`ProfilesApi` protocol + :class:`DefaultProfilesApi`.
- ``app.modules.profiles.schemas``: all request / response DTOs and cross-module DTOs.

Other files (models, repository, service) are module-private.
"""
