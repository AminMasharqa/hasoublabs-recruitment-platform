# Requirement 1 AC11: every dependency is declared with a pinned exact version.
save-exact=true
# openapi-typescript 7.x declares a `typescript@^5` peer while this workspace is on
# TypeScript 6; the tool itself works against the installed compiler.
legacy-peer-deps=true
